package main

import "fmt"

func main() {
	fmt.Println("init")
}

func drawRectangle(width, height int, outline, fill rune) {
	art := make([][]rune, 0)
	for x := 0; x < height; x++ {
		//first line
		line = make([]rune, width)
		if x == 0 {
			for i, v := range line{
				line[i]  =
			}
		}

	}
	art = append(art, )
}
